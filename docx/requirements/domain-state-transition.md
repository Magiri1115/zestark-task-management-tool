# ドメイン状態遷移図
## 状態一覧
### 業務状態
未着手
進行中
完了
### システム状態
ロック中
編集中（UI状態）
削除済（将来拡張）
## 状態遷移
```
未着手 → 進行中 → 完了
   ↑         ↓
   └─────────┘（差し戻し可）

任意状態
   ↓ 編集開始
ロック中
   ↓ 保存
元の業務状態へ戻る
```
## ロック状態遷移
```
未ロック
   ↓ lockTask()
ロック中
   ↓ updateTask()
未ロック
   ↓ 一定時間経過
未ロック（自動解除）
```